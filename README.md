#Hi!

This is my mma coaching app for CSC 491, my Independent Project.

first, run: venv/scripts/activate
next, run: pip install -r requirements.txt

1. to run react server: npm run dev 
    - needs to be run from /frontend
    - run "cd frontend"

2. to run api server: uvicorn api.main:app --reload